// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.zcu.fav.kiv.pia.slezacek.controller;

import java.io.UnsupportedEncodingException;
import java.lang.Integer;
import java.lang.Long;
import java.lang.String;
import java.util.ArrayList;
import java.util.Collection;
import java.util.List;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.joda.time.format.DateTimeFormat;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;
import org.zcu.fav.kiv.pia.slezacek.model.Tank;
import org.zcu.fav.kiv.pia.slezacek.model.Users;
import org.zcu.fav.kiv.pia.slezacek.model.Vehicle;

privileged aspect TankController_Roo_Controller {
    
    @RequestMapping(method = RequestMethod.POST)
    public String TankController.create(@Valid Tank tank, BindingResult result, Model model, HttpServletRequest request) {
        if (result.hasErrors()) {
            model.addAttribute("tank", tank);
            addDateTimeFormatPatterns(model);
            return "tanks/create";
        }
        tank.persist();
        return "redirect:/tanks/" + encodeUrlPathSegment(tank.getId().toString(), request);
    }
    
    @RequestMapping(params = "form", method = RequestMethod.GET)
    public String TankController.createForm(Model model) {
        model.addAttribute("tank", new Tank());
        addDateTimeFormatPatterns(model);
        List dependencies = new ArrayList();
        if (Users.countUserses() == 0) {
            dependencies.add(new String[]{"person", "userses"});
        }
        model.addAttribute("dependencies", dependencies);
        return "tanks/create";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.GET)
    public String TankController.show(@PathVariable("id") Long id, Model model) {
        addDateTimeFormatPatterns(model);
        model.addAttribute("tank", Tank.findTank(id));
        model.addAttribute("itemId", id);
        return "tanks/show";
    }
    
    @RequestMapping(method = RequestMethod.GET)
    public String TankController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model model) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            model.addAttribute("tanks", Tank.findTankEntries(page == null ? 0 : (page.intValue() - 1) * sizeNo, sizeNo));
            float nrOfPages = (float) Tank.countTanks() / sizeNo;
            model.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            model.addAttribute("tanks", Tank.findAllTanks());
        }
        addDateTimeFormatPatterns(model);
        return "tanks/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT)
    public String TankController.update(@Valid Tank tank, BindingResult result, Model model, HttpServletRequest request) {
        if (result.hasErrors()) {
            model.addAttribute("tank", tank);
            addDateTimeFormatPatterns(model);
            return "tanks/update";
        }
        tank.merge();
        return "redirect:/tanks/" + encodeUrlPathSegment(tank.getId().toString(), request);
    }
    
    @RequestMapping(value = "/{id}", params = "form", method = RequestMethod.GET)
    public String TankController.updateForm(@PathVariable("id") Long id, Model model) {
        model.addAttribute("tank", Tank.findTank(id));
        addDateTimeFormatPatterns(model);
        return "tanks/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE)
    public String TankController.delete(@PathVariable("id") Long id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model model) {
        Tank.findTank(id).remove();
        model.addAttribute("page", (page == null) ? "1" : page.toString());
        model.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/tanks?page=" + ((page == null) ? "1" : page.toString()) + "&size=" + ((size == null) ? "10" : size.toString());
    }
    
    @ModelAttribute("userses")
    public Collection<Users> TankController.populateUserses() {
        return Users.findAllUserses();
    }
    
    @ModelAttribute("vehicles")
    public Collection<Vehicle> TankController.populateVehicles() {
        return Vehicle.findAllVehicles();
    }
    
    void TankController.addDateTimeFormatPatterns(Model model) {
        model.addAttribute("tank_tankdate_date_format", DateTimeFormat.patternForStyle("S-", LocaleContextHolder.getLocale()));
    }
    
    String TankController.encodeUrlPathSegment(String pathSegment, HttpServletRequest request) {
        String enc = request.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        }
        catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
