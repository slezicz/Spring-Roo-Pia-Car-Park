// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.zcu.fav.kiv.pia.slezacek.controller;

import java.io.UnsupportedEncodingException;
import java.lang.Integer;
import java.lang.Long;
import java.lang.String;
import java.util.ArrayList;
import java.util.Collection;
import java.util.List;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.joda.time.format.DateTimeFormat;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;
import org.zcu.fav.kiv.pia.slezacek.model.Trip;
import org.zcu.fav.kiv.pia.slezacek.model.Users;
import org.zcu.fav.kiv.pia.slezacek.model.Vehicle;

privileged aspect TripController_Roo_Controller {
    
    @RequestMapping(method = RequestMethod.POST)
    public String TripController.create(@Valid Trip trip, BindingResult result, Model model, HttpServletRequest request) {
        if (result.hasErrors()) {
            model.addAttribute("trip", trip);
            addDateTimeFormatPatterns(model);
            return "trips/create";
        }
        trip.persist();
        return "redirect:/trips/" + encodeUrlPathSegment(trip.getId().toString(), request);
    }
    
    @RequestMapping(params = "form", method = RequestMethod.GET)
    public String TripController.createForm(Model model) {
        model.addAttribute("trip", new Trip());
        addDateTimeFormatPatterns(model);
        List dependencies = new ArrayList();
        if (Users.countUserses() == 0) {
            dependencies.add(new String[]{"person", "userses"});
        }
        model.addAttribute("dependencies", dependencies);
        return "trips/create";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.GET)
    public String TripController.show(@PathVariable("id") Long id, Model model) {
        addDateTimeFormatPatterns(model);
        model.addAttribute("trip", Trip.findTrip(id));
        model.addAttribute("itemId", id);
        return "trips/show";
    }
    
    @RequestMapping(method = RequestMethod.GET)
    public String TripController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model model) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            model.addAttribute("trips", Trip.findTripEntries(page == null ? 0 : (page.intValue() - 1) * sizeNo, sizeNo));
            float nrOfPages = (float) Trip.countTrips() / sizeNo;
            model.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            model.addAttribute("trips", Trip.findAllTrips());
        }
        addDateTimeFormatPatterns(model);
        return "trips/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT)
    public String TripController.update(@Valid Trip trip, BindingResult result, Model model, HttpServletRequest request) {
        if (result.hasErrors()) {
            model.addAttribute("trip", trip);
            addDateTimeFormatPatterns(model);
            return "trips/update";
        }
        trip.merge();
        return "redirect:/trips/" + encodeUrlPathSegment(trip.getId().toString(), request);
    }
    
    @RequestMapping(value = "/{id}", params = "form", method = RequestMethod.GET)
    public String TripController.updateForm(@PathVariable("id") Long id, Model model) {
        model.addAttribute("trip", Trip.findTrip(id));
        addDateTimeFormatPatterns(model);
        return "trips/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE)
    public String TripController.delete(@PathVariable("id") Long id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model model) {
        Trip.findTrip(id).remove();
        model.addAttribute("page", (page == null) ? "1" : page.toString());
        model.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/trips?page=" + ((page == null) ? "1" : page.toString()) + "&size=" + ((size == null) ? "10" : size.toString());
    }
    
    @ModelAttribute("userses")
    public Collection<Users> TripController.populateUserses() {
        return Users.findAllUserses();
    }
    
    @ModelAttribute("vehicles")
    public Collection<Vehicle> TripController.populateVehicles() {
        return Vehicle.findAllVehicles();
    }
    
    void TripController.addDateTimeFormatPatterns(Model model) {
        model.addAttribute("trip_tripdate_date_format", DateTimeFormat.patternForStyle("S-", LocaleContextHolder.getLocale()));
    }
    
    String TripController.encodeUrlPathSegment(String pathSegment, HttpServletRequest request) {
        String enc = request.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        }
        catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
